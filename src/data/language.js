// External dependencies

// Class Implementation
export default class Language {
    constructor(
        id,
        key,
        name,
        direction) {
           this.id = id
           this.key = key
           this.name = name
           this.direction = direction
       }
    }
